# 拼图识别与矩形恢复算法——重要部分精简说明

## 1. 系统核心目标

程序通过摄像头识别A4标定区域内的1～4片碎片，并把碎片恢复成矩形。最终输出每片碎片的：

- ID；
- 初始面积中心；
- 目标面积中心；
- 旋转角度。

整个恢复过程中只允许**旋转和平移**，不允许缩放，也不允许镜像。

---

## 2. 整体处理流程

```text
摄像头采集
→ 四角标定
→ 透视校正为840×1188
→ 黑色背景中分割碎片
→ 提取轮廓和面积中心
→ 判断固定四片 / 纯色 / 花纹
→ 执行对应矩形恢复算法
→ 检查重叠、空洞和填充率
→ 输出初始中心、目标中心和旋转角
```

---

## 3. 碎片识别算法

### 3.1 透视校正

用户点击标定方框四个角，程序将该区域统一映射成：

```text
840 × 1188 像素
```

A4纸实际尺寸为210 mm × 297 mm，因此校正比例为：

```text
4像素/mm
```

这样无论摄像头距离和角度如何变化，进入求解器的尺寸比例都保持一致。

### 3.2 亮度分割

程序将校正图转换为HSV颜色空间，使用V亮度通道分割亮色碎片和黑色背景。

处理步骤为：

```text
高斯滤波
→ Otsu自动阈值
→ 开运算去除小噪声
→ 轻量闭运算修补边缘
```

闭运算不能过强，否则靠得较近的两片碎片会被连接成一片。

### 3.3 轮廓过滤

每个轮廓需要满足：

- 面积在允许范围内；
- 旋转外接矩形短边不小于24像素；
- 能拟合为3～5边凸多边形。

程序对多个拟合系数进行尝试，并选择与原轮廓面积误差最小的多边形。

### 3.4 面积中心

碎片中心使用轮廓图像矩计算：

```text
center_x = M10 / M00
center_y = M01 / M00
```

识别中心、初始吸取位置和目标放置位置全部统一使用该**面积中心**。

---

## 4. 三类矩形恢复算法

## 4.1 固定四片恢复

固定四片具有已知模板。

算法流程：

1. 将识别轮廓转换为毫米坐标；
2. 将每片识别轮廓与四个模板逐一进行刚体配准；
3. 使用SVD计算最优旋转和平移；
4. 计算每组匹配的轮廓误差和面积误差；
5. 枚举4片与4个模板的全部24种分配；
6. 选择总代价最小的组合；
7. 将模板整体放到A4另一半区域；
8. 输出每片的目标面积中心和旋转角。

固定模式只使用旋转和平移，并通过旋转矩阵行列式检查禁止镜像。

---

## 4.2 未知纯色碎片自主恢复

纯色碎片没有固定模板，主要依靠几何边匹配。

算法不是简单地找到最接近的两条边后立即拼接，而是：

1. 预计算任意两片任意两条边的对接方式；
2. 优先尝试长度接近、重合长度较大的边；
3. 先搜索完整等长边匹配；
4. 必要时再允许部分边和T形接缝；
5. 使用深度优先搜索生成完整组合；
6. 对每个完整组合计算全局矩形质量；
7. 选择重叠最小、空洞最小、填充率最高的方案。

搜索过程中使用以下剪枝：

- 碎片之间不能出现明显面积重叠；
- 当前外接矩形不能超过允许尺寸；
- 当前布局不能松散到无法达到最低填充率；
- 相同旋转和位置状态不重复搜索；
- 达到高质量候选后提前结束。

---

## 4.3 花纹或扑克牌恢复

花纹模式在几何拼接基础上增加接缝图案连续性。

程序沿两片碎片的匹配接缝均匀采样，并分别向两片内部偏移，比较接缝两侧对应位置的LAB颜色差。

```text
纹理越连续
→ LAB颜色差越小
→ 纹理得分越低
→ 候选越优
```

扑克牌模式还会额外利用：

- 扑克牌圆角；
- 点数或字母；
- 标准长宽比；
- 外边和内部切边的区别；
- 极低重叠要求。

如果扑克牌专用算法失败，程序会退回通用几何加纹理恢复。

---

## 5. 全局矩形评分

对于一个完整候选，程序计算所有碎片的最小面积外接矩形。

### 5.1 填充率

```text
填充率 = 所有碎片面积总和 / 外接矩形面积
```

填充率越接近1，说明矩形内部空洞越少。

### 5.2 重叠与空洞

程序计算任意两片的交叠面积，并估算：

```text
空洞面积 = 矩形面积 - 碎片并集面积
```

布局误差主要由以下三项组成：

```text
空洞面积
+ 超出矩形面积
+ 2 × 碎片重叠面积
```

重叠的惩罚更高，因为重叠既会造成叠纸，也会在其他位置留下空洞。

### 5.3 最终选择

纯色模式主要依据几何分数。

花纹模式使用：

```text
总分 = 几何分数 + 纹理权重 × 接缝纹理分数
```

最终选择总分最低的方案。

---

## 6. 坐标系与角度

最终输出坐标系为：

- 原点：标定方框右下角；
- x轴：向上为正；
- y轴：向左为正。

校正图像内部点为 `(u, v)` 时：

```text
x = 1187 - v
y = 839 - u
```

四个角为：

```text
右下角：(0, 0)
右上角：(1187, 0)
左下角：(0, 839)
左上角：(1187, 839)
```

旋转角约定：

```text
顺时针：正值
逆时针：负值
```

例如：

```text
+90°：顺时针旋转90°
-90°：逆时针旋转90°
```

---

## 7. 目标位置生成

求解器先在内部毫米坐标中得到每片的旋转矩阵和平移量。

然后：

1. 将完整拼图旋转到水平或竖直方向；
2. 计算拼图整体尺寸；
3. 将目标矩形放到碎片所在A4半区的对侧；
4. 将每片初始面积中心执行相同刚体变换；
5. 得到该片目标面积中心；
6. 转换成右下角机械坐标输出。

因此目标位置并不是目标轮廓外接框中心，而是**碎片面积中心经过最终旋转和平移后的真实位置**。

---

## 8. 实时优化机制

以前按P后画面卡住，是因为求解器和摄像头显示运行在同一个主线程。

当前程序将求解放入后台线程：

```text
主线程：持续读取相机和刷新画面
后台线程：执行矩形搜索
```

求解过程中画面显示：

```text
SOLVING AUTO
SOLVING WHITE
SOLVING PATTERN
```

同时还进行了以下优化：

- 透视矩阵只在标定改变时重新计算；
- 完整碎片识别不必每一帧都执行；
- 求解期间降低识别频率，为搜索线程释放CPU；
- 固定、纯色、花纹算法不再重复全部运行；
- 限制搜索节点、候选数量和最长搜索时间；
- 花纹边缘图只计算一次并复用。

---

## 9. 最关键的验收条件

一个方案只有同时满足以下条件才会被认为恢复成功：

1. 所有碎片均已放置；
2. 碎片之间没有超过容差的重叠；
3. 拼图整体接近矩形；
4. 填充率达到最低要求；
5. 空洞面积和几何误差足够小；
6. 目标尺寸没有超过配置范围；
7. 每片变换均为合法旋转和平移；
8. 不存在缩放或镜像。

---

## 10. 核心总结

本系统的核心不是单独比较两条边，而是：

```text
标准化视觉识别
+ 多边形刚体建模
+ 全部可能边对接
+ 分阶段搜索和剪枝
+ 全局矩形评分
+ 花纹连续性辅助判断
+ 严格重叠和填充验收
```

最终输出完全统一为：

```text
初始面积中心
目标面积中心
逆时针为负的旋转角
右下角原点机械坐标
```
