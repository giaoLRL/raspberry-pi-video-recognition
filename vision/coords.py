"""
coords.py - ????????
================================
????????????
  ??:  ????? + y=300px (??75mm)
  X?(+): ?? (??????)
  Y?(+): ?? (?y=300??)

??????:
  1. image_to_arm   : ???? ? arm?? (pick?)
  2. solver_to_arm  : solver?? ? arm?? (place?)
  3. arm_to_warp    : arm?? ? warp?? (???)
"""

# ---- ???? (? pi_stream_puzzle_v2.py ????) ----
A4_WIDTH_MM  = 210.0   # A4 ??
A4_HEIGHT_MM = 297.0   # A4 ??
WARP_WIDTH   = 840      # warp ??? (px)
WARP_HEIGHT  = 1188     # warp ??? (px)
PIXELS_PER_MM = (WARP_WIDTH / A4_WIDTH_MM + WARP_HEIGHT / A4_HEIGHT_MM) * 0.5  # 4.0

# display/pick ????? warp ??????
ORIGIN_WX = WARP_WIDTH - 1      # ??? x ??
ORIGIN_WY = 300.0               # ??75mm = 300px


def image_to_arm(center_x_image: float, center_y_image: float) -> tuple[float, float]:
    """?????? ? arm ?? (pick/display ???)

    Args:
        center_x_image: ????? warp ???? x (px)
        center_y_image: ????? warp ???? y (px)

    Returns:
        (arm_x_mm, arm_y_mm): arm ??
          arm_x = ????, ???? (mm)
          arm_y = ? y=300 ?, ???? (mm)
    """
    x_mm = (ORIGIN_WX - center_x_image) / PIXELS_PER_MM
    y_mm = (center_y_image - ORIGIN_WY) / PIXELS_PER_MM
    return (x_mm, y_mm)


def solver_to_arm(solver_x: float, solver_y: float) -> tuple[float, float]:
    """solver ?? (TL??, X?, Y?) ? arm ?? (pick/display ???)

    Args:
        solver_x: solver ??? x (mm from left edge, rightward)
        solver_y: solver ??? y (mm from top edge, downward)

    Returns:
        (arm_x_mm, arm_y_mm): arm ?? (? image_to_arm ???)
    """
    x_mm = A4_WIDTH_MM - solver_x      # ?? = ?? - solver??
    y_mm = solver_y - (ORIGIN_WY / PIXELS_PER_MM)  # ?? = solver?? - 75mm
    return (x_mm, y_mm)


def arm_to_warp(x_mm: float, y_mm: float) -> tuple[float, float]:
    """arm ?? ? warp ???? (??????)

    Args:
        x_mm: arm X (??, mm)
        y_mm: arm Y (??, mm)

    Returns:
        (wx, wy): warp ????????
    """
    wx = ORIGIN_WX - x_mm * PIXELS_PER_MM
    wy = ORIGIN_WY + y_mm * PIXELS_PER_MM
    return (wx, wy)


def arm_distance(x_mm: float, y_mm: float) -> float:
    """?? arm ???????? (??????)"""
    import math
    return math.hypot(x_mm, y_mm)

